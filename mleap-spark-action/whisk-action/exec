#!/usr/bin/env scala

import com.jowanza.ExecutePipeline
import spray.json._


object ChatMain {

  def main(args: Array[String]): Unit = {

    val jsonString = args(0).parseJson.asJsObject.getFields("data").toString().toSeq

    print(ExecutePipeline.predictModel(jsonString))
  }
}





